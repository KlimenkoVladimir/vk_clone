import React, { FC } from "react";
import Header from "../Header";
import SideBar from "../SideBar";

const Home: FC = () => {
  return (
    <div>
      <Header></Header>
      <SideBar></SideBar>
    </div>
  );
};

export default Home;
